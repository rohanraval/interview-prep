/*
	Given two singly linked lists, determine if they intersect
		Intersection node is defined based on VALUE not reference
*/

//Approach 1: O(n^2) search through both lists to determine where intersection node is.

//Approach 2:

Node intersect(Node a, Node b) {


}
