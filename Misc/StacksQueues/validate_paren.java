/* Given input of brackets (paren, square, curly, etc) validate whether it has correct matching
 	ie. [{([])}] is valid but [{([}])} is NOT
*/

public boolean validate_paren(String s) {

	ArrayList<
	for(int i)
}
